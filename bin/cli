#!/usr/bin/env node
'use strict';

const twilio = require('../lib');
const cli = require('../lib/cli');

(async () => {
  cli.run();

  try {
    await twilio.run();
  } catch (err) {
    process.exit(1);
  }

  process.exit(0);
})();
